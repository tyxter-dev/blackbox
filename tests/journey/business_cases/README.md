# Business Case Journeys

Business case journeys validate domain workflows built on top of the runtime.
They are intentionally closer to examples than low-level unit tests: the goal
is to prove that application packages can compose tools, prompt fragments,
run plans, and parity checks into one inspectable workflow.

Run with:

```bash
pytest tests/journey/business_cases
```
